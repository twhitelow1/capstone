# app/views/api/photos/index.json.jb
@rooms.map do |room|
  {
    id: room.id,
    name: room.name,
    home_id: room.home_id,
    room_type: room.room_type,
    floor: room.floor,
    privacy: room.privacy,
  }
end
